/* import { strictEqual } from 'assert';
import SelzClient from '../';

describe('getProduct', () => {
    it('returns correct title', () => {
        const c = new SelzClient({ domain: 'local.selz.com' });

        console.warn(SelzClient.version());

        const p = c.getProduct('http://selz.co/1rvb96h');

        p.then(json => strictEqual(json.title, 'AIAIAI TMA-1 Pro Headphones')).catch(error => console.error(error));
    });
}); */
